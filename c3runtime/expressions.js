"use strict";
{
    globalThis.C3.Plugins.EMI_INDO_WebCloudStorage.Exps = {
        Url()
        {
            return this.url;
        },

        ErrorMsg()
        {
            return JSON.stringify(this.errorMsg);
        }
    };
}